// Prevent us from attempting to use variables
// that are not declared
"use strict"
/*
function reverseString(str) {
    const reversed = str.split('').reverse().join('');
    console.log(`The reversed string of "${str}" is: "${reversed}"`);
    return reversed;
}
reverseString("hello");  

function addNumbers(a, b) {
    const result = a + b;
    console.log(`The sum of ${a} and ${b} is:`, result);
    return result;
}
addNumbers(5, 10); 

function calculateSquare(num) {
    const square = num * num;
    return square; // This function returns a value
}
const square = calculateSquare(5); 
console.log(`Square of 5 is:`, square);

function greetUser(name) {
    console.log(`Hello, ${name}! Welcome to our platform.`); 
    // This function doesn't return a value
}
greetUser("Alice");

function greet(name = "Guest", greeting = "Hello") {
    console.log(`${greeting}, ${name}!`);
}

greet(); 
greet("Alice"); 
greet("Bob", "Welcome");

*/